rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ── DEVELOPMENT RULES — Wide open, no auth required ──────────────────────
    // TODO: After setting up Firebase Phone Auth properly, tighten these rules.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
