# St. Paul's ERP — FastAPI Backend Requirements
# Slim backend: Razorpay, MSG91, AI, PDF, Finance Reports

# Core
fastapi==0.110.1
uvicorn==0.25.0
python-dotenv>=1.0.1
pydantic>=2.6.4
python-multipart>=0.0.9

# Firebase Admin SDK (verify tokens + read Firestore for reports)
firebase-admin>=6.4.0

# Razorpay
razorpay>=1.3.0

# HTTP requests (MSG91 API)
requests>=2.31.0

# PDF Generation
reportlab>=4.1.0

# AI (Google Gemini — replaces emergentintegrations)
google-generativeai>=0.7.0

# Dev / Testing
pytest>=8.0.0
httpx>=0.27.0
