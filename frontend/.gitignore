# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# production
/build

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ── Capacitor / Android ──────────────────────────────────────────────────────
# Play Store signing keys — NEVER commit these!
*.jks
*.keystore
android/stpauls-release.jks

# Google services config (contains Firebase API keys)
# Add the real file from Firebase Console manually — don't commit it
android/app/google-services.json

# Android build outputs
android/.gradle/
android/app/build/
android/build/
